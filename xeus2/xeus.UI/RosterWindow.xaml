<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2006" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" xmlns:xeus2_xeus_Core="clr-namespace:xeus2.xeus.Core"
    x:Class="xeus2.RosterWindow"
    Title="xeus2" Height="300" Width="300"
    >
	<Window.Resources>
		<ObjectDataProvider x:Key="RosterDS" d:IsDataSource="True" ObjectType="{x:Type xeus2_xeus_Core:Roster}"/>
		<ObjectDataProvider x:Key="ServicesDS" d:IsDataSource="True" ObjectType="{x:Type xeus2_xeus_Core:Services}"/>
	</Window.Resources>
    <StackPanel>
      <ListBox ItemsSource="{Binding Path=Instance, Mode=Default, Source={StaticResource RosterDS}}" Height="100"/>
      <ListBox Height="100" ItemsSource="{Binding Path=Instance, Mode=Default, Source={StaticResource ServicesDS}}"/>
    </StackPanel>
</Window>
