<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:xeus2_xeus_Controls="clr-namespace:xeus2.xeus.UI.xeus.UI.Controls"
    x:Class="xeus2.xeus.UI.xeus.UI.Controls.MultiTabControl"
   x:Name="UserControl"
    >
	<UserControl.Resources>
    <DataTemplate x:Key="MultiTabItem" DataType="{x:Type xeus2_xeus_Controls:MultiTabItem}">
		<StackPanel>
        <TextBlock Text="{Binding Path=Name}" Foreground="{DynamicResource TextBoxBorderBrush}" MinWidth="200" TextWrapping="Wrap" VerticalAlignment="Center"/>
		<CheckBox IsChecked="{Binding Path=IsVisible}" Content="Is Visible"/>
		</StackPanel>
    </DataTemplate>	
	</UserControl.Resources>
    <DockPanel>
      <ListBox Background="{DynamicResource back_x_design}" Width="100" 
               ItemsPanel="{DynamicResource ItemsPanelTemplateRoster}"
			   ItemsSource="{Binding}"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               x:Name="_tabs" ItemTemplate="{DynamicResource MultiTabItem}" BorderThickness="0,0,0,0"/>
      <DockPanel x:Name="_container" DockPanel.Dock="Right" Focusable="True"/>
    </DockPanel>
</UserControl>
